package screen

const (
	// MessageModified indicates that the Node has been modified after initialization.
	MessageModified   = "the node should not be modified after initialization"
	// MessageNewElement indicates that a new element has been added after initialization.
	MessageNewElement = "no new elements should be added after initialization"
)
